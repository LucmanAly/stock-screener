# Normalise to LF in the repository regardless of platform. Without this, the
# nightly rewrite of data.csv can churn line endings and inflate every diff.
* text=auto eol=lf
*.csv text eol=lf
*.py text eol=lf
*.md text eol=lf
