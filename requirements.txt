# Direct dependencies, pinned to the versions this project was built and tested against.
# Recreate the environment with:
#   python -m venv venv
#   venv\Scripts\python.exe -m pip install -r requirements.txt     (Windows)
#   venv/bin/python -m pip install -r requirements.txt             (macOS / Linux)
#
# Requires Python 3.11+ (built and verified on 3.14.1).

lxml==6.1.3
numpy==2.5.3
pandas==3.0.5
requests==2.34.2
tabulate==0.10.0
yfinance==1.7.0
